/**
 * 📱 智能设备选择配置文件
 * 用于控制小程序端RSSI滤波和智能设备识别的所有参数
 * 开发者可通过修改这些参数来优化设备选择算法的表现
 */

const DeviceSelectionConfig = {
  // ===== RSSI 信号强度判断参数 =====
  
  /**
   * "明显更强"的RSSI阈值差值（dBm）
   * 当一个设备比其他设备强出此差值时，认为是明显领先
   * 调试建议：
   * - 设置过低(如3-5dBm)：容易在相近设备间频繁切换推荐
   * - 设置过高(如12-15dBm)：只有信号差距很大才会推荐，更保守
   * - 推荐值：8dBm，适合大部分环境
   */
  RSSI_STRONG_THRESHOLD: 8,

  /**
   * 极近距离RSSI阈值（贴手机背面）
   * 当RSSI高于此值时，认为设备极可能贴在手机背面
   * 调试建议：
   * - 设置过低(如-50dBm)：可能误判远距离强信号设备
   * - 设置过高(如-30dBm)：只有贴得很近才识别，可能漏检
   * - 推荐值：-40dBm，适合典型硬件功率
   */
  RSSI_PROXIMITY_THRESHOLD: -50,

  /**
   * 候选设备最小RSSI值
   * 低于此值的设备直接忽略，不参与智能选择
   * 调试建议：
   * - 设置过低(如-85dBm)：包含太多远距离噪声设备
   * - 设置过高(如-60dBm)：可能过滤掉一些有效设备
   * - 推荐值：-70dBm，过滤掉信号很弱的设备
   */
  RSSI_CANDIDATE_MIN: -60,

  // ===== 🎯 新增：KISS设备绑定相对优势参数 =====

  /**
   * 🎯 设备绑定：相对优势领先阈值（dBm）
   * 目标设备需比第二强设备持续领先此差值才推荐绑定
   * 解决不同手机蓝牙灵敏度差异问题，不依赖绝对RSSI阈值
   * 调试建议：
   * - 设置过低(如5-7dBm)：可能推荐信号差异不明显的设备
   * - 设置过高(如15-20dBm)：要求过于严格，很难找到合适设备
   * - 推荐值：10dBm，确保明显的相对优势
   */
  RSSI_DOMINANCE_THRESHOLD: 10,

  /**
   * 🎯 设备绑定：持续领先时间要求（毫秒）
   * 相对优势需持续保持此时间才确认为绑定目标
   * 避免瞬时信号波动导致的误判
   * 调试建议：
   * - 设置过短(如2000ms)：可能因信号抖动误推荐
   * - 设置过长(如8000ms)：用户等待时间过长
   * - 推荐值：5000ms，平衡稳定性和响应速度
   */
  RSSI_DOMINANCE_TIME: 5000,

  /**
   * 🎯 设备绑定：绝对最低信号要求（dBm）
   * 即使是相对最强的设备也需满足此基本信号要求
   * 过滤信号过于微弱的设备，确保连接可靠性
   * 调试建议：
   * - 设置过高(如-65dBm)：可能过滤掉一些有效但信号稍弱的设备
   * - 设置过低(如-85dBm)：包含信号过弱难以稳定连接的设备
   * - 推荐值：-75dBm，保证基本连接质量
   */
  RSSI_ABSOLUTE_MIN: -75,

  /**
   * 🎯 设备绑定：单设备场景最低要求（dBm）
   * 只发现一个设备时的推荐最低信号要求
   * 比多设备场景要求略高，确保唯一设备质量可靠
   * 调试建议：
   * - 设置过高(如-60dBm)：单设备场景难以推荐
   * - 设置过低(如-80dBm)：可能推荐信号质量不佳的设备
   * - 推荐值：-70dBm，确保单设备连接稳定性
   */
  RSSI_SINGLE_DEVICE_MIN: -70,

  /**
   * 🎯 设备绑定：快速推荐超时（毫秒）
   * 在此时间内找到明显领先设备时立即推荐
   * 优化用户体验，快速识别贴近设备
   * 调试建议：
   * - 设置过短(如3000ms)：可能错过后出现的更优设备
   * - 设置过长(如12000ms)：失去快速推荐的优势
   * - 推荐值：8000ms，给足时间发现最优设备
   */
  BINDING_FAST_TIMEOUT: 8000,

  /**
   * 🎯 设备绑定：保底推荐超时（毫秒）
   * 超过此时间后推荐当前最稳定的设备，避免无限等待
   * 确保在复杂环境下用户也能完成绑定流程
   * 调试建议：
   * - 设置过短(如8000ms)：可能推荐不够稳定的设备
   * - 设置过长(如20000ms)：用户等待时间过长，体验差
   * - 推荐值：12000ms，平衡等待时间和推荐质量
   */
  BINDING_FALLBACK_TIMEOUT: 12000,

  /**
   * 🎯 设备绑定：放宽的稳定性要求（dBm标准差）
   * 考虑到绑定场景的特殊性，适当放宽稳定性要求
   * 适应更多手机硬件环境的信号特性
   * 调试建议：
   * - 设置过低(如3.0dBm)：要求过于严格，很难满足
   * - 设置过高(如10.0dBm)：可能接受信号很不稳定的设备
   * - 推荐值：6.0dBm，平衡稳定性要求和硬件兼容性
   */
  RSSI_STABILITY_RELAXED: 6.0,

  // ===== RSSI 滤波和稳定性参数 =====

  /**
   * RSSI历史记录数量（用于移动平均滤波）
   * 越多的样本能提供更稳定的滤波结果，但响应会更慢
   * 调试建议：
   * - 设置过少(如3-4)：滤波效果差，容易受瞬时干扰
   * - 设置过多(如15-20)：响应太慢，用户等待时间长
   * - 推荐值：8，平衡稳定性和响应速度
   */
  RSSI_HISTORY_SIZE: 4,

  /**
   * RSSI标准差阈值（dBm）
   * 小于此值认为信号稳定，可以用于推荐
   * 调试建议：
   * - 设置过低(如1.5dBm)：要求过于苛刻，很难达到
   * - 设置过高(如6dBm)：即使信号很不稳定也会推荐
   * - 推荐值：3.0dBm，适合一般环境的信号稳定性
   */
  RSSI_STABILITY_THRESHOLD:8.0,

  /**
   * 需要持续稳定的时间（毫秒）
   * 设备需要观察此时间才能建立足够的置信度
   * 调试建议：
   * - 设置过短(如2000ms)：可能基于不充分数据做决策
   * - 设置过长(如8000ms)：用户等待时间过长，体验差
   * - 推荐值：4000ms，给足够时间收集稳定数据
   */
  RSSI_STABLE_TIME: 3000,

  /**
   * 最小置信度阈值（0-1之间）
   * 只有置信度超过此值的设备才会被推荐
   * 调试建议：
   * - 设置过低(如0.6)：可能推荐不够稳定的设备
   * - 设置过高(如0.95)：要求过于严格，很少能推荐
   * - 推荐值：0.8，要求较高的置信度才推荐
   */
  RSSI_CONFIDENCE_MIN: 0.6,

  // ===== 推荐撤回控制参数 =====

  /**
   * 撤回推荐的置信度阈值
   * 当已推荐设备的置信度降到此值以下时，撤回推荐
   * 调试建议：
   * - 设置过低(如0.4)：很少撤回推荐，可能保持不稳定推荐
   * - 设置过高(如0.85)：频繁撤回推荐，用户体验不稳定
   * - 推荐值：0.6，低于推荐阈值但给一定缓冲
   */
  REVOCATION_CONFIDENCE_THRESHOLD: 0.4,

  // ===== 算法行为控制参数 =====

  /**
   * 最少样本数要求
   * 少于此样本数的设备不参与稳定性计算
   * 调试建议：
   * - 设置过少(如2)：基于很少数据就判断稳定性
   * - 设置过多(如8)：需要等待很久才开始判断
   * - 推荐值：4，确保有基本的统计意义
   */
  MIN_SAMPLES_FOR_STABILITY: 4,

  /**
   * 等待明显强信号设备出现的时间（毫秒）
   * 在此时间内，算法会等待是否有明显更强的设备出现
   * 调试建议：
   * - 设置过短(如1000ms)：可能错过后出现的更强设备
   * - 设置过长(如8000ms)：用户等待时间过长
   * - 推荐值：3000ms，给足够时间发现所有附近设备
   */
  DEVICE_SELECTION_WAIT_TIME: 3000,

  // ===== 置信度计算权重 =====

  /**
   * 时间因子权重（0-1之间）
   * 在置信度计算中，观察时间长度的权重
   */
  CONFIDENCE_WEIGHT_TIME: 0.4,

  /**
   * 稳定性因子权重（0-1之间）
   * 在置信度计算中，信号稳定性的权重
   */
  CONFIDENCE_WEIGHT_STABILITY: 0.4,

  /**
   * 样本数因子权重（0-1之间）
   * 在置信度计算中，历史样本数量的权重
   */
  CONFIDENCE_WEIGHT_SAMPLES: 0.2,

  // ===== 调试和日志控制 =====

  /**
   * 是否启用详细的设备选择日志
   * true: 输出详细的算法决策过程，便于调试
   * false: 只输出关键结果，减少日志噪声
   */
  ENABLE_SELECTION_DEBUG_LOG: true,

  /**
   * 是否在界面显示调试信息
   * true: 在设备列表中显示RSSI历史、稳定性等调试信息
   * false: 只显示基本的设备信息
   */
  ENABLE_UI_DEBUG_INFO: true,

  // ===== 性能优化参数 =====

  /**
   * 设备历史清理周期（毫秒）
   * 定期清理长时间未更新的设备历史数据
   */
  DEVICE_HISTORY_CLEANUP_INTERVAL: 30000,

  /**
   * 设备历史保留时间（毫秒）
   * 超过此时间未更新的设备历史将被清理
   */
  DEVICE_HISTORY_RETENTION_TIME: 60000
};

// 导出配置对象
module.exports = DeviceSelectionConfig;

/**
 * 🔧 调试指南
 * 
 * === 🎯 KISS设备绑定调试指南 (新增) ===
 * 
 * 1. 设备绑定识别太严格，很难推荐设备：
 *    - 降低 RSSI_DOMINANCE_THRESHOLD (如改为7-8dBm)
 *    - 减少 RSSI_DOMINANCE_TIME (如改为3000-4000ms)
 *    - 降低 RSSI_ABSOLUTE_MIN (如改为-80dBm)
 *    - 增加 RSSI_STABILITY_RELAXED (如改为8.0dBm)
 * 
 * 2. 设备绑定推荐不准确，经常推荐远距离设备：
 *    - 增加 RSSI_DOMINANCE_THRESHOLD (如改为12-15dBm)
 *    - 提高 RSSI_ABSOLUTE_MIN (如改为-70dBm)
 *    - 提高 RSSI_SINGLE_DEVICE_MIN (如改为-65dBm)
 *    - 增加 RSSI_DOMINANCE_TIME (如改为6000-7000ms)
 * 
 * 3. 设备绑定等待时间过长：
 *    - 减少 BINDING_FAST_TIMEOUT (如改为5000-6000ms)
 *    - 减少 BINDING_FALLBACK_TIMEOUT (如改为8000-10000ms)
 *    - 减少 RSSI_DOMINANCE_TIME (如改为3000-4000ms)
 * 
 * 4. iPhone用户绑定效果很好，但安卓用户很难绑定：
 *    - 降低 RSSI_ABSOLUTE_MIN (如改为-80dBm)
 *    - 降低 RSSI_DOMINANCE_THRESHOLD (如改为8dBm)
 *    - 增加 RSSI_STABILITY_RELAXED (如改为8.0dBm)
 * 
 * 5. 单设备环境下很难推荐：
 *    - 降低 RSSI_SINGLE_DEVICE_MIN (如改为-75dBm)
 *    - 减少单设备等待时间（在算法中控制）
 * 
 * === 原有设备选择调试指南 ===
 * 
 * 6. 信号太敏感，频繁切换推荐：
 *    - 增加 RSSI_STRONG_THRESHOLD (如改为10-12)
 *    - 增加 RSSI_STABLE_TIME (如改为5000-6000)
 *    - 降低 RSSI_STABILITY_THRESHOLD (如改为2.0-2.5)
 * 
 * 7. 很难触发推荐，总是在等待：
 *    - 降低 RSSI_CONFIDENCE_MIN (如改为0.7-0.75)
 *    - 降低 RSSI_STABILITY_THRESHOLD (如改为4.0-5.0)
 *    - 减少 RSSI_STABLE_TIME (如改为3000)
 * 
 * 8. 推荐了错误的远距离设备：
 *    - 提高 RSSI_PROXIMITY_THRESHOLD (如改为-35)
 *    - 增加 RSSI_STRONG_THRESHOLD (如改为10)
 *    - 提高 RSSI_CANDIDATE_MIN (如改为-65)
 * 
 * 9. 贴近的设备识别不出来：
 *    - 降低 RSSI_PROXIMITY_THRESHOLD (如改为-45)
 *    - 降低 RSSI_CONFIDENCE_MIN (如改为0.7)
 *    - 减少 MIN_SAMPLES_FOR_STABILITY (如改为3)
 * 
 * 10. 算法响应太慢：
 *    - 减少 RSSI_HISTORY_SIZE (如改为6)
 *    - 减少 RSSI_STABLE_TIME (如改为3000)
 *    - 减少 MIN_SAMPLES_FOR_STABILITY (如改为3)
 * 
 * 修改这些参数后，重新启动小程序即可生效，无需重新编译ESP32固件。
 * 
 * 🎯 KISS设备绑定的核心优势：
 * - 不依赖绝对RSSI阈值，解决手机硬件差异问题
 * - 基于相对优势判定，适应各种信号环境
 * - 智能超时机制，平衡准确性和用户等待时间
 * - 参数配置灵活，便于不同场景调优
 */