/* miniprogram/pages/connect/connect.wxss */
/**connect.wxss**/

/* 公共样式类 */
.base-black-bg {
  background-color: #000;
}

.base-white-border {
  border: 1px solid rgba(255, 255, 255, 0.3);
}

.base-transition {
  transition: all 0.3s ease;
}

.base-card {
  background-color: #000;
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 16rpx;
  transition: all 0.3s ease;
}

/* 通用动画效果 */
@keyframes base-tap-bounce {
  0%, 100% {
    transform: translateY(0) scale(1);
    box-shadow: 0 0 8rpx currentColor, 0 0 16rpx currentColor;
  }
  50% {
    transform: translateY(-4rpx) scale(1.05);
    box-shadow: 0 0 12rpx currentColor, 0 0 24rpx currentColor, 0 0 36rpx currentColor;
  }
}

.base-tap-animation {
  animation: base-tap-bounce 2.5s infinite ease-in-out;
}

.container {
  padding: 0;
  font-size: 14px;
  color: #fff; /* 文字颜色改为白色 */
  position: relative;
  min-height: 100vh; /* 改为min-height确保可以显示更多内容 */
  background-color: #000; /* 背景改为黑色 */
  display: flex; /* 添加flex布局 */
  flex-direction: column; /* 垂直排列 */
}

/* 主题背景图片 */
.theme-background {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  z-index: -2;
}

/* 背景虚化层调整为毛玻璃效果 */
.background-blur {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  backdrop-filter: blur(40px) saturate(1.2);
  -webkit-backdrop-filter: blur(40px) saturate(1.2);
  background-color: rgba(0, 0, 0, 0.6); /* 半透明黑色 */
  z-index: -1;
}

/* 页面顶部导航栏 */
.nav-bar {
  position: fixed;
  top: 100rpx;
  left: 0;
  right: 0;
  height: 120rpx;
  background-color: linear-gradient(135deg, #667eea, #764ba2);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding-top: env(safe-area-inset-top);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.nav-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #ffffff;
  text-align: center;
}

.nav-subtitle {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.7);
  margin-top: 4rpx;
}

.content {
  width: 100%;
  min-height: 100%; /* 改为min-height确保内容能完整显示 */
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  position: relative;
  padding-top: 88rpx; /* 为顶部导航栏留出空间 */
  flex: 1; /* 允许容器自适应扩展 */
}

/* 连接文字块 - 现代设计 */
.connection-text-block {
  width: 260rpx;
  height: 260rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 50%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  position: relative;
  box-shadow: 0 16rpx 60rpx rgba(102, 126, 234, 0.4);
  transition: all 0.3s ease;
}

.connection-text-block:hover {
  transform: scale(1.05);
  box-shadow: 0 20rpx 80rpx rgba(102, 126, 234, 0.6);
}

/* 连接文字 */
.connection-number {
  font-size: 110rpx;
  font-weight: 700;
  color: #ffffff;
  line-height: 1;
  margin-bottom: 8rpx;
  text-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.3);
}

/* 连接标签 */
.connection-label {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.9);
  margin-top: 0;
  display: flex;
  align-items: center;
  font-weight: 500;
}

.connection-label::before {
  content: "连接";
  font-size: 22rpx;
  margin-right: 4rpx;
}

.connection-label::after {
  content: "人";
  font-size: 22rpx;
  margin-left: 4rpx;
}

/* 便签部分 - 毛玻璃效果 */
.notes-container {
  width: 100%;
  background: linear-gradient(135deg, rgba(26, 26, 26, 0.95) 0%, rgba(13, 17, 23, 0.95) 100%);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-radius: 40rpx 40rpx 0 0;
  position: absolute;
  bottom: 0;
  left: 0;
  z-index: 10;
  overflow: hidden;
  transition: all 0.3s ease;
  box-shadow: 0 -8rpx 40rpx rgba(0, 0, 0, 0.3);
  border-top: 1px solid rgba(102, 126, 234, 0.3);
}

.notes-container.expanded {
  background: linear-gradient(135deg, rgba(26, 26, 26, 0.98) 0%, rgba(13, 17, 23, 0.98) 100%);
  backdrop-filter: blur(30px);
  -webkit-backdrop-filter: blur(30px);
}

.handle {
  width: 100rpx;
  height: 6rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 6rpx;
  margin: 24rpx auto;
  opacity: 0.7;
}

/* 卡片滚动区域 */
.cards-scroll-view {
  width: 100%;
  height: calc(100% - 40rpx); /* 减去handle的高度和margin */
}

/* 卡片容器 - 流式布局 */
.cards-container {
  padding: 20rpx;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

/* 卡片项 */
.card-item {
  width: 46%;
  margin-bottom: 24rpx;
  padding: 24rpx;
  border-radius: 20rpx;
  border: 1px solid rgba(102, 126, 234, 0.3);
  background: linear-gradient(135deg, #1a1a1a 0%, #161b22 100%);
  box-shadow: 0 8rpx 30rpx rgba(102, 126, 234, 0.15);
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.card-item:hover {
  transform: translateY(-6rpx);
  box-shadow: 0 12rpx 40rpx rgba(102, 126, 234, 0.3);
  border-color: rgba(102, 126, 234, 0.6);
}

/* 添加发光效果 */
.card-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2rpx;
  background: linear-gradient(90deg, transparent, rgba(102, 126, 234, 0.6), transparent);
  animation: glow 2s ease-in-out infinite alternate;
}

@keyframes glow {
  from {
    opacity: 0.5;
  }
  to {
    opacity: 1;
  }
}

/* 卡片点击效果 */
.card-item:active {
  transform: scale(0.98);
  transition: transform 0.1s ease;
  box-shadow: none;
}

/* 便利的触摸反馈 */
.card-item {
  -webkit-tap-highlight-color: rgba(102, 126, 234, 0.2);
}

/* 卡片头像区域 */
.card-avatar-area {
  display: flex;
  justify-content: center;
  margin-bottom: 16rpx;
}

.card-avatar-placeholder {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  background-color: #000; /* 改为黑色背景 */
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 48rpx;
  font-weight: bold;
  color: #fff;
  border: 1px solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
}

/* 卡片信息 */
.card-info {
  text-align: center;
}

.card-name {
  font-size: 30rpx;
  font-weight: 600;
  margin-bottom: 12rpx;
  color: #ffffff;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.card-position {
  font-size: 24rpx;
  color: #c9d1d9;
  margin-bottom: 8rpx;
  font-weight: 500;
}

.card-company {
  font-size: 22rpx;
  color: #8b949e;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* 卡片标签样式 */
.card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 12rpx;
  max-height: 80rpx;
  overflow: hidden;
  position: relative;
}

/* 标签渐出效果 */
.card-tags::after {
  content: '';
  position: absolute;
  bottom: 0;
  right: 0;
  width: 40rpx;
  height: 100%;
  background: linear-gradient(to left, rgba(26, 26, 26, 1), transparent);
  pointer-events: none;
}

.tag {
  font-size: 20rpx;
  padding: 6rpx 16rpx;
  border-radius: 16rpx;
  max-width: 140rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.2) 0%, rgba(118, 75, 162, 0.2) 100%);
  color: #667eea;
  border: 1px solid rgba(102, 126, 234, 0.3);
  font-weight: 500;
  border: 1px solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
}

.skill-tag, .field-tag {
  background-color: #000; /* 改为黑色背景 */
  color: #fff; /* 改为白色 */
  border: 1px solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
}

.tag-more {
  font-size: 18rpx;
  color: rgba(255, 255, 255, 0.4); /* 改为低透明度白色 */
  padding: 4rpx 0;
}

/* 用户统计信息 */
.user-stats {
  margin-top: 8rpx;
}

.stats-text {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.7);
  font-weight: 400;
}

/* 用户详细信息弹窗 */
.user-detail-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(15rpx);
  animation: modalFadeIn 0.3s ease-out;
}

.user-detail-content {
  position: relative;
  width: 90%;
  max-width: 600rpx;
  max-height: 80vh;
  background: linear-gradient(135deg, #0d1117 0%, #1a1a1a 100%);
  border-radius: 24rpx;
  padding: 40rpx;
  overflow-y: auto;
  border: 1px solid rgba(255, 255, 255, 0.15);
  box-shadow: 0 20rpx 60rpx rgba(0, 0, 0, 0.5), 0 0 0 1rpx rgba(255, 255, 255, 0.1);
  animation: slideUpFadeIn 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

@keyframes slideUpFadeIn {
  from {
    opacity: 0;
    transform: translateY(50rpx) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.user-detail-close {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  color: rgba(255, 255, 255, 0.7);
  background-color: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  z-index: 10;
  transition: all 0.2s ease;
}

.user-detail-close:active {
  transform: scale(0.9);
  background-color: rgba(255, 255, 255, 0.15);
  color: rgba(255, 255, 255, 0.9);
}

/* 用户基本信息 */
.user-detail-header {
  display: flex;
  align-items: center;
  margin-bottom: 40rpx;
  padding-bottom: 30rpx;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.user-detail-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  margin-right: 30rpx;
  border: 2px solid rgba(255, 255, 255, 0.2);
}

.user-detail-info {
  flex: 1;
}

.user-detail-name {
  font-size: 36rpx;
  font-weight: bold;
  color: #fff;
  margin-bottom: 8rpx;
}

.user-detail-subtitle {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.7);
  margin-bottom: 8rpx;
}

.user-detail-stats {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.6);
}

/* 标签区域 */
.user-detail-tags {
  margin-bottom: 30rpx;
}

.tag-section {
  margin-bottom: 30rpx;
}

.tag-section-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #fff;
  margin-bottom: 16rpx;
  padding-left: 8rpx;
  border-left: 4rpx solid #00d4ff;
}

.tag-list {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.tag-item {
  padding: 8rpx 16rpx;
  background-color: rgba(255, 255, 255, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 20rpx;
  font-size: 24rpx;
  color: #fff;
}

/* 联系信息 */
.user-detail-contact {
  margin-bottom: 30rpx;
  padding: 20rpx;
  background-color: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.contact-title {
  font-size: 26rpx;
  font-weight: bold;
  color: #fff;
  margin-bottom: 12rpx;
}

.contact-info {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
}

/* 个性标签 */
.user-detail-personal {
  margin-bottom: 30rpx;
  padding: 20rpx;
  background-color: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.personal-title {
  font-size: 26rpx;
  font-weight: bold;
  color: #fff;
  margin-bottom: 12rpx;
}

.personal-text {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.5;
}

/* 微信二维码 */
.user-detail-qrcode {
  margin-bottom: 20rpx;
  padding: 20rpx;
  background-color: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.qrcode-title {
  font-size: 26rpx;
  font-weight: bold;
  color: #fff;
  margin-bottom: 16rpx;
  text-align: center;
}

.qrcode-container {
  display: flex;
  justify-content: center;
  align-items: center;
}

.qrcode-image {
  width: 200rpx;
  height: 200rpx;
  border-radius: 12rpx;
  border: 2px solid rgba(255, 255, 255, 0.2);
  transition: all 0.3s ease;
}

.qrcode-image:active {
  transform: scale(1.05);
  border-color: rgba(255, 255, 255, 0.4);
  box-shadow: 0 0 20rpx rgba(255, 255, 255, 0.2);
}

.qrcode-hint {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.6);
  text-align: center;
  margin-top: 16rpx;
}



/* 歌单式名片列表 - 更改为黑色背景 */
.music-playlist {
  width: 100%;
  height: calc(100vh - 200rpx); /* 优化高度计算，给导航栏和缓冲区留出合理空间 */
  min-height: 600rpx; /* 设置最小高度确保基本显示 */
  max-height: calc(100vh - 150rpx); /* 设置最大高度避免内容溢出屏幕 */
  background-color: #000; /* 改为黑色背景 */
  border-radius: 40rpx 40rpx 0 0;
  box-shadow: none;
  position: relative;
  z-index: 5;
  padding-top: 20rpx;
  border-top: 1px solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
}

/* 歌单容器 - 改为滚轮效果 */
.playlist-container {
  width: 100%;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0;
  height: 100%;
  /* 移除overflow:hidden限制，允许内容完整显示 */
}

/* 新增滚动轨道样式 */
.track-wheel {
  width: 100%;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  transition: none; /* 移除过渡动画以提高性能 */
  will-change: transform; /* 优化性能 */
  transform: translateZ(0); /* 启用硬件加速 */
}

/* 列表顶部和底部填充 */
.list-padding {
  width: 100%;
  height: 200rpx; /* 进一步减少填充高度，增加可显示内容区域 */
  flex-shrink: 0;
}

/* 添加顶部和底部渐变蒙版 - 调整为黑色渐变 */
.music-playlist::before,
.music-playlist::after {
  content: '';
  position: absolute;
  left: 0;
  right: 0;
  z-index: 4;
  pointer-events: none;
}

.music-playlist::before {
  top: 0;
  height: 100rpx; /* 减少顶部渐变高度 */
  background: linear-gradient(to bottom, #000, rgba(0, 0, 0, 0)); /* 黑色渐变 */
  border-radius: 40rpx 40rpx 0 0;
}

.music-playlist::after {
  bottom: 0;
  height: 120rpx; /* 减少底部渐变高度 */
  background: linear-gradient(to top, #000, rgba(0, 0, 0, 0)); /* 黑色渐变 */
}

/* 标签容器，优化多标签显示 */
.track-tags {
  display: flex;
  flex-wrap: wrap; /* 🔥 修复：允许标签换行显示 */
  margin-top: 8rpx;
  gap: 8rpx 12rpx; /* 设置标签间距：垂直8rpx，水平12rpx */
  max-height: 120rpx; /* 限制最大高度，避免占用过多空间 */
  overflow: hidden; /* 隐藏超出高度限制的部分 */
}

/* 碰一碰相关样式 - 优化布局 */
.track-title-container {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-wrap: wrap; /* 🔥 修复：允许标题和标识换行，防止挤压 */
  min-height: 48rpx; /* 确保有足够的高度 */
}

.touch-badge {
  display: inline-flex;
  align-items: center;
  background: linear-gradient(135deg, #ff6b6b, #ffd93d);
  padding: 4rpx 12rpx;
  border-radius: 20rpx;
  gap: 4rpx;
}

.touch-icon {
  font-size: 20rpx;
}

.touch-text {
  font-size: 20rpx;
  color: #fff;
  font-weight: bold;
}

/* 匹配的标签样式 */
.matched-tags .tag.matched {
  background: linear-gradient(135deg, #667eea, #764ba2);
  color: #fff;
  border: 1px solid rgba(255, 255, 255, 0.3);
}

/* 碰一碰时间 */
.touch-time {
  display: flex;
  align-items: center;
  margin-top: 8rpx;
  font-size: 22rpx;
}

.touch-time-label {
  color: rgba(255, 255, 255, 0.6);
}

.touch-time-value {
  color: rgba(255, 255, 255, 0.8);
  margin-left: 4rpx;
}

/* 单个歌曲项 - 改为黑色风格 */
.track-item {
  width: 90%;
  padding: 30rpx;
  margin: 30rpx 0;
  height: 230rpx;
  border-radius: 24rpx;
  background-color: #000; /* 改为黑色背景 */
  transition: all 0.3s ease;
  transform-origin: center;
  position: relative;
  display: flex;
  align-items: center;
  z-index: 3;
  box-sizing: border-box;
  transform: translateZ(0);
  box-shadow: none;
  border: 1px solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
}

/* 聚焦的歌曲项 */
.track-item.active {
  background-color: #000; /* 保持黑色背景 */
  transform: scale(1.08) translateZ(0);
  box-shadow: none;
  z-index: 10;
  border: 1px solid rgba(255, 255, 255, 0.6); /* 边框更明显 */
}

/* 新增专辑封面容器 */
.album-cover-container {
  width: 180rpx; /* 略微减小封面尺寸，留更多文字空间 */
  height: 180rpx; /* 略微减小封面尺寸，留更多文字空间 */
  margin-right: 30rpx; /* 减少右边距 */
  flex-shrink: 0; /* 防止被压缩 */
  position: relative;
  border-radius: 8rpx; /* 小圆角，保持方形感 */
  overflow: hidden;
  box-shadow: 0 6rpx 12rpx rgba(0, 0, 0, 0.1); /* 添加阴影 */
}

/* 专辑封面 */
.album-cover {
  width: 100%;
  height: 100%;
  object-fit: cover; /* 确保图片填满区域 */
}

/* 专辑封面占位符 */
.album-cover-placeholder {
  width: 100%;
  height: 100%;
  background-color: #000; /* 改为黑色背景 */
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 60rpx; /* 大字体 */
  font-weight: bold;
  color: rgba(255, 255, 255, 0.4); /* 改为低透明度白色 */
  border: 1px solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
}

/* 歌曲信息区域 - 改进布局防止溢出 */
.track-info {
  display: flex;
  flex-direction: column;
  flex: 1;
  height: 100%;
  justify-content: center;
  overflow: visible; /* 🔥 修复：允许内容正常显示，不强制裁剪 */
  max-width: calc(100% - 180rpx); /* 🔥 修复：增加可用宽度，从210rpx减少到180rpx */
  min-width: 0; /* 确保flex子元素可以收缩 */
}

.track-title {
  font-size: 40rpx;
  font-weight: bold;
  color: #fff; /* 改为白色 */
  margin-bottom: 10rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.track-artist {
  font-size: 32rpx;
  color: rgba(255, 255, 255, 0.6); /* 改为半透明白色 */
  margin-bottom: 8rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.track-position {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.4); /* 改为低透明度白色 */
  margin-bottom: 12rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* 标签区域，类似歌曲风格标签 - 优化显示 */
.tag {
  font-size: 24rpx;
  padding: 6rpx 16rpx;
  border-radius: 30rpx;
  margin: 0; /* 🔥 修复：移除margin，使用gap统一管理间距 */
  background-color: #000; /* 改为黑色背景 */
  color: #fff; /* 改为白色文字 */
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
  max-width: 160rpx; /* 🔥 修复：增加最大宽度，从120rpx增加到160rpx */
  flex-shrink: 0; /* 防止标签被过度压缩 */
  border: 1rpx solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
}

.tag-more {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.6); /* 🔥 修复：提高透明度，增强可读性 */
  padding: 6rpx 12rpx; /* 🔥 修复：添加内边距，与标签样式保持一致 */
  background-color: rgba(255, 255, 255, 0.1); /* 添加半透明背景 */
  border-radius: 30rpx; /* 与标签保持一致的圆角 */
  align-self: center;
  white-space: nowrap;
  flex-shrink: 0; /* 防止被压缩 */
}

/* 修改加载状态颜色 */
.loading-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 400rpx;
}

.loading-icon {
  width: 80rpx;
  height: 80rpx;
  border: 4rpx solid rgba(255, 255, 255, 0.1);
  border-top: 4rpx solid #ff7e5f;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 20rpx;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loading-text {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.4); /* 改为低透明度白色 */
}

/* 错误状态 */
.error-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80rpx 0;
}

.error-text {
  font-size: 28rpx;
  color: #ff5252;
  margin-bottom: 20rpx;
  text-align: center;
}

.retry-btn {
  padding: 16rpx 40rpx;
  background: linear-gradient(135deg, #ff7e5f, #feb47b);
  color: white;
  font-size: 28rpx;
  border-radius: 40rpx;
}

/* 空状态 */
.empty-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 100rpx 0;
}

.empty-icon {
  font-size: 100rpx;
  margin-bottom: 30rpx;
  color: #b3b3b3;
}

.empty-text {
  font-size: 36rpx;
  font-weight: bold;
  color: #fff; /* 保持白色 */
  margin-bottom: 20rpx;
}

.empty-subtext {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.4); /* 改为低透明度白色 */
  text-align: center;
  max-width: 80%;
}

/* 名片预览弹窗 */
.card-preview-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.7);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.card-preview-content {
  width: 90%;
  max-width: 650rpx;
  position: relative;
}

.card-preview-close {
  position: absolute;
  top: 20rpx;
  right: 30rpx;
  font-size: 50rpx;
  color: rgba(255, 255, 255, 0.6);
  z-index: 10;
  font-weight: normal;
  cursor: pointer;
  transition: all 0.3s ease;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
}

.card-preview-close:hover {
  color: #ff6b6b;
  background: rgba(255, 255, 255, 0.1);
}

.business-card {
  background-color: #000; /* 改为黑色背景 */
  border-radius: 20rpx;
  overflow: hidden;
  box-shadow: none;
  border: 1px solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
}

.card-header {
  padding: 40rpx;
  display: flex;
  align-items: center;
  background-color: #000; /* 改为黑色背景 */
}

.card-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  margin-right: 30rpx;
  border: 1px solid rgba(255, 255, 255, 0.3); /* 添加白色边框 */
  object-fit: cover; /* 确保图片填满区域 */
  background-color: #000; /* 改为黑色背景 */
}

.card-basic-info {
  flex: 1;
}

.card-name {
  font-size: 36rpx;
  font-weight: bold;
  color: #fff; /* 改为白色 */
  margin-bottom: 10rpx;
}

.card-position, .card-organization {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.6); /* 改为半透明白色 */
  margin-bottom: 6rpx;
}

.card-divider {
  height: 2rpx;
  background-color: rgba(255, 255, 255, 0.3); /* 改为半透明白色 */
}

.card-body {
  padding: 40rpx;
}

.card-intro {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.7); /* 改为半透明白色 */
  margin-bottom: 30rpx;
  line-height: 1.6;
}

.card-section-title {
  font-size: 28rpx;
  color: #fff; /* 改为白色 */
  font-weight: bold;
  margin-bottom: 15rpx;
  margin-top: 20rpx;
}

.card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 15rpx;
  margin-bottom: 20rpx;
}

.card-tag {
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.2) 0%, rgba(118, 75, 162, 0.2) 100%);
  color: #667eea;
  font-size: 24rpx;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  border: 1px solid rgba(102, 126, 234, 0.4);
  font-weight: 500;
  transition: all 0.2s ease;
}

.card-tag:hover {
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.3) 0%, rgba(118, 75, 162, 0.3) 100%);
  border-color: rgba(102, 126, 234, 0.6);
  transform: translateY(-2rpx);
}

.card-contact-value {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.7); /* 改为半透明白色 */
}

/* 新增主题相关样式 */
.card-concept-text {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.7);
  margin-bottom: 20rpx;
  line-height: 1.6;
  font-style: italic;
}

.card-philosophy-text {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.7);
  margin-bottom: 20rpx;
  line-height: 1.6;
  font-weight: 500;
}

/* 移除旧的唱片点击效果 */

/* 星星样式 */
.star {
  position: absolute;
  background-color: #fff;
  border-radius: 50%;
  z-index: 2;
  box-shadow: 0 0 10rpx rgba(255, 255, 255, 0.8);
  animation: twinkle 4s infinite ease-in-out;
}

@keyframes twinkle {
  0%, 100% { opacity: 0.2; }
  50% { opacity: 0.7; }
}

/* 配对用户展示区 */
.pair-section {
  width: 90%;
  margin: 40rpx auto 0 auto;
  background: rgba(0,0,0,0.7);
  border-radius: 24rpx;
  padding: 30rpx 0 20rpx 0;
  box-shadow: 0 4rpx 24rpx rgba(0,0,0,0.18);
  border: 1px solid rgba(255,255,255,0.15);
  display: flex;
  flex-direction: column;
  align-items: center;
}
.pair-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #fff;
  margin-bottom: 20rpx;
  letter-spacing: 2rpx;
}
.pair-list {
  display: flex;
  flex-direction: row;
  justify-content: center;
  gap: 40rpx;
}
.pair-user {
  display: flex;
  flex-direction: column;
  align-items: center;
  cursor: pointer;
}

.pair-user-container {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.pair-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  border: 2rpx solid #fff;
  margin-bottom: 10rpx;
  object-fit: cover;
  background: #222;
}

.pair-tap-hint {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  background: transparent;
  border: 2rpx solid;
  border-radius: 20rpx;
  padding: 4rpx 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: tap-bounce 2s infinite ease-in-out;
  z-index: 10;
  /* 添加发光效果 */
  box-shadow: 0 0 8rpx currentColor, 0 0 16rpx currentColor;
  filter: drop-shadow(0 0 6rpx currentColor);
}

.tap-text {
  font-size: 18rpx;
  font-weight: bold;
}

@keyframes tap-bounce {
  0%, 100% {
    transform: translateY(0);
    box-shadow: 0 0 8rpx currentColor, 0 0 16rpx currentColor;
  }
  50% {
    transform: translateY(-4rpx);
    box-shadow: 0 0 12rpx currentColor, 0 0 24rpx currentColor, 0 0 36rpx currentColor;
  }
}

.pair-nickname {
  font-size: 26rpx;
  color: #fff;
  text-align: center;
  max-width: 120rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 配对理由弹窗 */
.pair-modal {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  z-index: 2000;
  display: flex;
  align-items: center;
  justify-content: center;
}
.pair-modal-mask {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.7);
  z-index: 1;
}
.pair-modal-content {
  position: relative;
  z-index: 2;
  background: #111;
  border-radius: 24rpx;
  padding: 60rpx 40rpx 40rpx 40rpx;
  min-width: 500rpx;
  max-width: 90vw;
  box-shadow: 0 8rpx 32rpx rgba(0,0,0,0.25);
  display: flex;
  flex-direction: column;
  align-items: center;
}
.pair-modal-close {
  position: absolute;
  top: 20rpx;
  right: 30rpx;
  font-size: 48rpx;
  color: #fff;
  z-index: 3;
  font-weight: bold;
}
.pair-modal-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 30rpx;
}
.pair-modal-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  border: 2rpx solid #fff;
  margin-bottom: 10rpx;
  object-fit: cover;
  background: #222;
}
.pair-modal-nickname {
  font-size: 30rpx;
  color: #fff;
  font-weight: bold;
  margin-bottom: 10rpx;
}
.pair-modal-reason-title {
  font-size: 28rpx;
  color: #fff;
  font-weight: bold;
  margin-bottom: 10rpx;
  letter-spacing: 2rpx;
}
.pair-modal-reason {
  font-size: 26rpx;
  color: #fff;
  line-height: 1.7;
  text-align: center;
  word-break: break-all;
  max-width: 90vw;
}

/* 社群成员展示样式 */
.card-members {
  margin-top: 20rpx;
}



.member-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 15rpx;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12rpx;
  transition: all 0.3s ease;
  min-width: 100rpx;
}

.member-item:hover {
  background: rgba(255, 255, 255, 0.1);
  transform: translateY(-2rpx);
}

.member-avatar {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  margin-bottom: 8rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.2);
}

.member-name {
  font-size: 22rpx;
  color: #ffffff;
  text-align: center;
  max-width: 80rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 弹窗滚动区域样式 */
.card-body-scroll {
  max-height: 60vh; /* 最大高度为视窗高度的60% */
  min-height: 200rpx; /* 最小高度 */
  overflow-y: auto;
  /* 添加渐变阴影提示滚动 */
  background: linear-gradient(rgba(17, 17, 17, 1) 0%, rgba(17, 17, 17, 0.9) 100%);
}

/* 滚动容器的滚动条样式（在支持的平台上） */
.card-body-scroll::-webkit-scrollbar {
  width: 4rpx;
}

.card-body-scroll::-webkit-scrollbar-track {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 2rpx;
}

.card-body-scroll::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.3);
  border-radius: 2rpx;
}

.card-body-scroll::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.5);
}

/* 优化弹窗整体布局 */
.card-preview-content {
  max-height: 85vh; /* 弹窗整体最大高度 */
  overflow: hidden; /* 防止弹窗超出屏幕 */
}

.business-card {
  display: flex;
  flex-direction: column;
  max-height: 100%;
  overflow: hidden;
}

/* 确保成员网格在滚动容器中正常显示 */
.members-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
  margin-top: 10rpx;
  padding-bottom: 20rpx; /* 添加底部间距，避免最后一行被切断 */
}

/* 滚动提示样式 */
.scroll-hint {
  margin-top: 30rpx;
  text-align: center;
  padding: 20rpx;
  border-top: 1rpx solid rgba(255, 255, 255, 0.1);
}

.scroll-hint-text {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.5);
  opacity: 0.8;
}

/* ============ 简易名片列表样式 ============ */

/* 简易名片列表容器 */
.member-cards-list {
  display: flex;
  flex-direction: column;
  gap: 15rpx;
  margin-top: 10rpx;
}

/* 单个简易名片横条 */
.member-mini-card {
  display: flex;
  align-items: center;
  padding: 20rpx;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  transition: all 0.3s ease;
  cursor: pointer;
}

.member-mini-card:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: rgba(255, 255, 255, 0.2);
  transform: translateX(5rpx);
}

.member-mini-card:active {
  transform: scale(0.98) translateX(5rpx);
}

/* 简易名片头像容器 */
.member-avatar-container {
  position: relative;
  margin-right: 20rpx;
  flex-shrink: 0;
}

/* 简易名片头像 */
.member-mini-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  border: 2rpx solid rgba(255, 255, 255, 0.2);
}

/* 社群成员"碰"提示 */
.member-tap-hint {
  position: absolute;
  top: -6rpx;
  right: -6rpx;
  background: transparent;
  border: 2rpx solid;
  border-radius: 16rpx;
  padding: 3rpx 6rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: member-tap-bounce 2.5s infinite ease-in-out;
  z-index: 5;
  /* 添加发光效果 */
  box-shadow: 0 0 6rpx currentColor, 0 0 12rpx currentColor;
  filter: drop-shadow(0 0 4rpx currentColor);
}

.member-tap-text {
  font-size: 16rpx;
  font-weight: bold;
}

@keyframes member-tap-bounce {
  0%, 100% {
    transform: translateY(0) scale(1);
    box-shadow: 0 0 6rpx currentColor, 0 0 12rpx currentColor;
  }
  50% {
    transform: translateY(-3rpx) scale(1.05);
    box-shadow: 0 0 10rpx currentColor, 0 0 20rpx currentColor, 0 0 30rpx currentColor;
  }
}

/* 简易名片信息区 */
.member-mini-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  min-width: 0; /* 允许内容收缩 */
}

.member-mini-name {
  font-size: 28rpx;
  font-weight: bold;
  color: #ffffff;
  margin-bottom: 4rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.member-mini-status {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.6);
}

/* 简易名片箭头 */
.member-mini-arrow {
  font-size: 30rpx;
  color: rgba(255, 255, 255, 0.4);
  margin-left: 10rpx;
  transition: all 0.3s ease;
  flex-shrink: 0;
}

.member-mini-card:hover .member-mini-arrow {
  color: rgba(255, 255, 255, 0.8);
  transform: translateX(5rpx);
}

/* ============ 详细名片弹窗样式 ============ */

/* 详细名片弹窗遮罩 */
.detailed-card-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.85);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2000;
  animation: fadeIn 0.3s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* 详细名片遮罩点击区域 */
.detailed-card-mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1;
}

/* 详细名片内容容器 */
.detailed-card-content {
  position: relative;
  z-index: 2;
  background: linear-gradient(145deg, #1a1a1a, #0d0d0d);
  border-radius: 24rpx;
  box-shadow: 0 12rpx 48rpx rgba(0, 0, 0, 0.4);
  width: 85vw;
  max-width: 550rpx;
  max-height: 80vh;
  overflow: hidden;
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  animation: slideUp 0.3s ease-out;
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(50rpx) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* 详细名片关闭按钮 */
.detailed-card-close {
  position: absolute;
  top: 20rpx;
  right: 30rpx;
  font-size: 50rpx;
  color: rgba(255, 255, 255, 0.6);
  z-index: 10;
  font-weight: normal;
  cursor: pointer;
  transition: all 0.3s ease;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
}

.detailed-card-close:hover {
  color: #ff6b6b;
  background: rgba(255, 255, 255, 0.1);
}

/* 详细名片头部 */
.detailed-card-header {
  display: flex;
  align-items: center;
  padding: 50rpx 40rpx 30rpx 40rpx;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.1);
}

/* 详细名片头像容器 */
.detailed-card-avatar-container {
  position: relative;
  margin-right: 30rpx;
}

.detailed-card-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  border: 3rpx solid rgba(255, 255, 255, 0.2);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.3);
}

/* 详细名片"碰"提示 */
.detailed-card-tap-hint {
  position: absolute;
  top: -10rpx;
  right: -10rpx;
  background: transparent;
  border: 2rpx solid;
  border-radius: 22rpx;
  padding: 6rpx 10rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: detailed-tap-bounce 2.2s infinite ease-in-out;
  z-index: 15;
  /* 添加发光效果 */
  box-shadow: 0 0 10rpx currentColor, 0 0 20rpx currentColor;
  filter: drop-shadow(0 0 8rpx currentColor);
}

.detailed-tap-text {
  font-size: 20rpx;
  font-weight: bold;
}

@keyframes detailed-tap-bounce {
  0%, 100% {
    transform: translateY(0) scale(1);
    box-shadow: 0 0 10rpx currentColor, 0 0 20rpx currentColor;
  }
  50% {
    transform: translateY(-5rpx) scale(1.08);
    box-shadow: 0 0 15rpx currentColor, 0 0 30rpx currentColor, 0 0 45rpx currentColor;
  }
}

.detailed-card-info {
  flex: 1;
}

.detailed-card-name {
  font-size: 36rpx;
  font-weight: bold;
  color: #ffffff;
  margin-bottom: 8rpx;
}

.detailed-card-status {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.7);
  margin-bottom: 8rpx;
}

.detailed-card-theme {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.5);
}

/* 详细名片分割线 */
.detailed-card-divider {
  height: 1rpx;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  margin: 0;
}

/* 详细名片滚动区域 */
.detailed-card-body-scroll {
  max-height: 50vh;
  overflow-y: auto;
  background: linear-gradient(rgba(26, 26, 26, 1) 0%, rgba(13, 13, 13, 0.95) 100%);
}

.detailed-card-body-scroll::-webkit-scrollbar {
  width: 4rpx;
}

.detailed-card-body-scroll::-webkit-scrollbar-track {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 2rpx;
}

.detailed-card-body-scroll::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.2);
  border-radius: 2rpx;
}

.detailed-card-body-scroll::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.4);
}

/* 详细名片内容 */
.detailed-card-body {
  padding: 40rpx;
}

.card-section {
  margin-bottom: 40rpx;
}

.card-section:last-child {
  margin-bottom: 0;
}

.card-intro-text {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.6;
  text-align: left;
}

/* 标签网格 */
.card-tags-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 15rpx;
}

.card-tag-item {
  padding: 8rpx 16rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 20rpx;
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.8);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

/* AI匹配理由卡片 */
.match-reason-card {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  padding: 30rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  margin-top: 15rpx;
  min-height: 120rpx;
}

.match-reason-content {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.6;
  text-align: left;
}

.match-reason-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 20rpx 0;
}

.loading-dots {
  display: flex;
  gap: 8rpx;
  margin-bottom: 16rpx;
}

.loading-dots .dot {
  width: 8rpx;
  height: 8rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.6);
  animation: loading-pulse 1.4s infinite ease-in-out;
}

.loading-dots .dot:nth-child(1) {
  animation-delay: -0.32s;
}

.loading-dots .dot:nth-child(2) {
  animation-delay: -0.16s;
}

.loading-dots .dot:nth-child(3) {
  animation-delay: 0s;
}

@keyframes loading-pulse {
  0%, 80%, 100% {
    transform: scale(0.8);
    opacity: 0.5;
  }
  40% {
    transform: scale(1);
    opacity: 1;
  }
}

.match-reason-loading text {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.6);
}

.match-reason-placeholder {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.5);
  text-align: center;
  padding: 20rpx 0;
  font-style: italic;
}

/* 操作按钮 */
.card-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 40rpx;
  padding-top: 30rpx;
  border-top: 1rpx solid rgba(255, 255, 255, 0.1);
}

.action-btn {
  flex: 1;
  padding: 24rpx 0;
  border-radius: 12rpx;
  text-align: center;
  font-size: 28rpx;
  font-weight: bold;
  transition: all 0.3s ease;
  cursor: pointer;
}

.action-btn.primary {
  background: linear-gradient(135deg, #667eea, #764ba2);
  color: #ffffff;
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.action-btn.primary:hover {
  background: linear-gradient(135deg, #5a6fd8, #6a42a0);
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 24rpx rgba(102, 126, 234, 0.3);
}

.action-btn.secondary {
  background: rgba(255, 255, 255, 0.1);
  color: rgba(255, 255, 255, 0.8);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.action-btn.secondary:hover {
  background: rgba(255, 255, 255, 0.15);
  color: #ffffff;
  transform: translateY(-2rpx);
}

/* 名片信息缺失提示样式 */
.card-empty-hint {
  text-align: center;
  padding: 40rpx 20rpx;
  margin-top: 20rpx;
  background: rgba(255, 255, 255, 0.03);
  border: 2rpx dashed rgba(255, 255, 255, 0.1);
  border-radius: 16rpx;
}

.empty-hint-icon {
  font-size: 48rpx;
  margin-bottom: 12rpx;
}

.empty-hint-text {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.8);
  margin-bottom: 8rpx;
  font-weight: 500;
}

.empty-hint-sub {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.5);
  line-height: 1.4;
}

/* 匹配标签高亮样式 */
.tag-item.matched {
  background: linear-gradient(135deg, #7b68ee, #00d4ff) !important;
  color: #ffffff !important;
  font-weight: bold;
  border: 2rpx solid rgba(255, 255, 255, 0.3);
  box-shadow: 0 4rpx 12rpx rgba(123, 104, 238, 0.3);
  transform: scale(1.05);
}

/* ===== 分离显示样式 ===== */

/* 分区标题样式 */
.section-divider {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx;
  margin: 16rpx 0;
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.1));
  border-radius: 16rpx;
  border: 1rpx solid rgba(102, 126, 234, 0.3);
}

.section-divider.collapsible {
  cursor: pointer;
  transition: all 0.3s ease;
}

.section-divider.collapsible:active {
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.2), rgba(118, 75, 162, 0.2));
  transform: scale(0.98);
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #ffffff;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.collapse-icon {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  transition: all 0.3s ease;
}

.collapse-icon.expanded {
  transform: rotate(90deg);
  background: rgba(102, 126, 234, 0.2);
}

.collapse-icon text {
  font-size: 24rpx;
  color: #667eea;
  font-weight: bold;
}

/* 未注册设备区域样式 */
.unregistered-section {
  margin: 24rpx 0;
}

.unregistered-devices-container {
  animation: slideDown 0.3s ease-out;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-20rpx);
    max-height: 0;
  }
  to {
    opacity: 1;
    transform: translateY(0);
    max-height: 1000rpx;
  }
}

/* 未注册设备特殊样式 */
.track-item.unregistered-item {
  opacity: 0.8;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.05), rgba(150, 150, 150, 0.05));
  border: 1rpx solid rgba(150, 150, 150, 0.2);
}

.track-title.unregistered {
  color: #999 !important;
}

.track-artist.unregistered {
  color: #666 !important;
}

.stats-text.unregistered {
  color: #888 !important;
}

/* 未注册设备标识 */
.unregistered-badge {
  display: flex;
  align-items: center;
  gap: 8rpx;
  background: rgba(255, 165, 0, 0.2);
  border: 1rpx solid rgba(255, 165, 0, 0.4);
  border-radius: 12rpx;
  padding: 4rpx 12rpx;
  margin-left: 16rpx;
}

.unregistered-icon {
  font-size: 20rpx;
}

.unregistered-text {
  font-size: 20rpx;
  color: #ffa500;
  font-weight: 500;
}

.tag.unregistered {
  background: rgba(150, 150, 150, 0.2);
  color: #999;
  border: 1rpx solid rgba(150, 150, 150, 0.3);
}